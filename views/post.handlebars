<!--post details-->
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.contents}}
    </p>
    <p>Created by {{post.user.name}} on {{post.datse_created}}</p>
</div>

<!--comment form-->
<div>
    <form class="form create-post-form">
    <div class="form-group">
        <label for="post-title">Comment</label>
        <input class="form-input" type="text" id="comment-body" name="comment-body" />
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Post Comment</button>
    </div>
    </form>
</div>

<!--diplay comments-->
<div>
    {{#each comments as |comment| }}
    <p>
      {{coment.contents}}
    </p>
    <p>Created by {{comment.user.name}} on {{comment.date_created}}</p>
    {{/each}}
</div>

    <script src="./js/post.js"></script>